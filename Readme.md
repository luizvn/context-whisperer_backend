# Backend do projeto Context-whisperer 

Ponto de entrada para o projeto é o main.ts na função `bootstrap()`

Módulo de agentes definido no `/agents`

## Rodando o banco

> $ npm db:generate

> $ npm db:migrate