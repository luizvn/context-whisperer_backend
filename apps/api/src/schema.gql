# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""Os tipos de artefatos que o sistema pode gerar"""
enum ArtifactType {
  API_SPEC
  ARCHITECTURE_DOC
  DOMAIN_MODEL
  REQUIREMENTS
  UML_DIAGRAM
  USER_STORIES
}

type AuthResponse {
  accessToken: String!
  user: User!
}

input CreateProjectInput {
  artifacts: [ArtifactType!]!
  name: String!
  prompt: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type JobQueuedResponse {
  jobId: String!
  requisitionId: String!
  status: String!
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  """
  Inicia o fluxo de criação de um novo projeto via LangGraph inserindo na fila
  """
  createProject(input: CreateProjectInput!): JobQueuedResponse!
  login(loginInput: LoginInput!): AuthResponse!
  signup(signupInput: SignupInput!): AuthResponse!
}

type Query {
  me: User!
}

type ScopeProposal {
  contentMd: String!
  createdAt: DateTime!
  id: ID!
  requisitionId: String!
  status: ScopeProposalStatus!
  userFeedback: String
}

enum ScopeProposalStatus {
  APPROVED
  PENDING
  REJECTED
}

input SignupInput {
  email: String!
  name: String!
  password: String!
}

type Subscription {
  """Recebe eventos de agentes para o usuário (via parâmetro livre)"""
  agentEvents(userId: String!): ScopeProposal
}

type User {
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String!
  role: String!
}